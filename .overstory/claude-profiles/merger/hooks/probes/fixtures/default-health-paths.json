{
  "$schema": "candidate health paths probed in priority order until one returns 2xx",
  "apiHealth": [
    "/api/v1/health",
    "/api/health",
    "/health",
    "/healthz",
    "/status"
  ],
  "webHealth": [
    "/",
    "/healthz",
    "/api/health"
  ]
}
